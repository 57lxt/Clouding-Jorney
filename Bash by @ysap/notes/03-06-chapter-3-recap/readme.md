# Chapter 3 Recap
## File descriptors
- &0 - stdin
- &1 - stdout
- &2 - stderror
     
usage:
```bash
echo "ERORR: file not found" >&2 #redirecting the msg to stderror
```

## notes
- ```$#``` - how many arguments the user gave us
- exit 1 - we can't use return in a function, so we use exit
- wildcards - d* - means begins with d
- 

## Commands to try
```help '((' ``` or ```help let```
